#include <msp430.h>
#include "libTimer.h"
#include "buzzer.h"

int main() {
    configureClocks();
 
    buzzer_init();
    buzzer_set_period();// start buzzing!!! 2MHz/1000 = 2kHz*/
    enableWDTInterrupts();
    or_sr(0x18);          // CPU off, GIE on
}
